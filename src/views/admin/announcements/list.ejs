<% layout('layout') %>
<div class="d-flex justify-content-between align-items-center mb-3">
  <h3 class="mb-0">Announcements</h3>
  <a class="btn btn-sm btn-outline-success" href="/admin">Dashboard</a>
</div>
<div class="row g-3 mb-4">
  <div class="col-md-8">
    <div class="p-3 border rounded">
      <h6 class="mb-2">Create Announcement</h6>
      <form method="post" action="/admin/announcements/create">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>"/>
        <div class="mb-2"><input class="form-control" name="title" placeholder="title" required></div>
        <div class="mb-2"><textarea class="form-control" name="body" rows="3" placeholder="body" required></textarea></div>
        <div class="form-check mb-2"><input class="form-check-input" type="checkbox" id="isPublic" name="isPublic" checked><label for="isPublic" class="form-check-label">Public</label></div>
        <button class="btn btn-success">Publish</button>
      </form>
    </div>
  </div>
</div>
<div class="table-responsive">
  <table class="table table-sm table-striped">
    <thead><tr><th>Title</th><th>Public</th><th>Date</th><th>Actions</th></tr></thead>
    <tbody>
      <% announcements.forEach(a => { %>
        <tr>
          <td><%= a.title %></td>
          <td><%= a.isPublic ? 'Yes' : 'No' %></td>
          <td><%= new Date(a.createdAt).toLocaleString() %></td>
          <td>
            <form method="post" action="/admin/announcements/<%= a.id %>/delete" onsubmit="return confirm('Delete announcement?')">
              <input type="hidden" name="_csrf" value="<%= csrfToken %>"/>
              <button class="btn btn-sm btn-outline-danger">Delete</button>
            </form>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</div>
